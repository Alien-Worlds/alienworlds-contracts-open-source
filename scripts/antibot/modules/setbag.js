
module.exports = (set_bot, whitelist) => {

    const setbag = async (data) => {
        // if (whitelist.includes(data.account)){
        //     return
        // }

        console.log('setbag', data)
    }

    return { setbag }
}
